{
  "name": "uk-music-experiences-scripts",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Seed scripts for the UK Music Experiences PocketBase setup",
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "verify": "node scripts/verify.mjs",
    "faker": "node scripts/faker.mjs",
    "app:dev": "npm --prefix app run dev",
    "app:build": "cross-env BASE_PATH= npm --prefix app run build",
    "app:build:uk": "cross-env BASE_PATH=/uk npm --prefix app run build",
    "app:test": "npm --prefix app run test",
    "caddy": "caddy run"
  },
  "dependencies": {
    "pocketbase": "^0.26.8"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
